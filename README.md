# FileExplorer
A small file explorer program written in Python (GUI in tkinter). Explore folders and open files. Note: it is minimalist and doesn't include many functions like file operations or other stuff. 
**NOTE: i lost the code somewhere this is closed-source sry**
